# 项目规则

## Android 正式安装包版本

- Android 正式安装包的展示版本号使用一位小数，例如 `6.0`、`6.1`、`6.2`。
- 每次执行正式 APK 打包时自动递增小数位：`6.0 → 6.1 → … → 6.9 → 7.0`。
- Android `versionCode` 在每次正式打包时同步增加 `1`，确保新安装包可以覆盖安装旧版本。
- 只有 APK 构建和签名全部成功后，才保留递增后的版本号；构建或签名失败不得占用版本号。
- 正式 APK 文件名必须包含对应展示版本号，例如 `LQChat-v6.1-android-arm64.apk`。
- 普通编译、调试构建、测试构建和验收构建不得递增正式版本号。
- 自动递增版本时不得修改应用包名、Android 权限或现有签名密钥。
